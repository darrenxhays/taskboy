services:
  dev:
    # full image, not -slim: tests exercise the real git cli (workspace pre-push hook)
    image: python:3.12
    working_dir: /app
    volumes:
      - .:/app
    environment:
      - ENVIRONMENT=local
